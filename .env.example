# Google Search Console — HTML tag verification (Search Console → Ownership → HTML tag)
# Copy only the content value, not the full meta tag.
# Example: abc123XYZ_from_google
VITE_GSC_VERIFICATION=

# Careers applications inbox (Supabase + admin UI at /admin/applications)
SUPABASE_URL=https://dgzfombeyvgldlshzzzq.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
CAREERS_ADMIN_PASSWORD=

# Set to false to skip post-build prerender (faster local builds)
# PRERENDER=false
